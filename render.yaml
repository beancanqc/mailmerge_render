# Render Deployment Configuration
# This file tells Render how to deploy your application

# Build Command (install dependencies)
pip install -r requirements.txt

# Start Command (run the application)
gunicorn app:app