# Mail Merge SaaS Requirements - Lightweight & Fast
# Install with: pip install -r requirements.txt

# Core web framework
flask==2.3.3
werkzeug==2.3.7

# Document processing (pure Python - no compilation)
python-docx==0.8.11
openpyxl==3.1.2

# PDF conversion (pure Python - no external dependencies)
reportlab==4.4.4

# Word native PDF conversion (Windows/macOS)
docx2pdf==0.1.8

# Production server
gunicorn==21.2.0