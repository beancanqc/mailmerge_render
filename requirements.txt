# Mail Merge SaaS Requirements - Enhanced with Word automation
# Install with: pip install -r requirements.txt

# Core web framework
flask==2.3.3
werkzeug==2.3.7

# Document processing (pure Python - no compilation)
python-docx==0.8.11
openpyxl==3.1.2

# PDF conversion (pure Python - no external dependencies)
mammoth==1.11.0

# Windows COM automation for Word (Windows only - optional)
pywin32==306

# Production server
gunicorn==21.2.0